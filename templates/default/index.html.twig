{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-md-6 offset-md-3 mt-5">
            {{ form_start(form) }}
                <label for="">Fichier:</label>
                {{form_widget(form.file, {label: "Fichier"})}}
                {{form_errors(form.file)}}
                <input type="submit" class="mt-2 btn btn-primary" value="Envoyer">
            {{ form_end(form) }}
        </div>
    </div>

</div>

{% endblock %}
